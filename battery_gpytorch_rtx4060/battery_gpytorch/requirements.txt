# Install PyTorch with CUDA 12.4 support first (for RTX 4060):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#
# Then install the rest:
#   pip install -r requirements.txt

gpytorch>=1.11
matplotlib>=3.7
numpy>=1.24
scikit-learn>=1.3
